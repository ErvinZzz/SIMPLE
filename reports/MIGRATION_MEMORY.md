---
name: project_simple_isaacsim51_migration
description: "SIMPLE benchmark migrated to IsaacSim 5.1 + MuJoCo 3.9 + Python 3.11 (RTX 5090); eval runs end-to-end, official DP scores 0/3 due to IsaacSim-5.x convex-cook bug (env, not model)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 1756a1db-f66c-41a5-afd3-8fbdf4563c15
---

Migrated physical-superintelligence-lab/SIMPLE (@ d010f0f) to **IsaacSim 5.1.0 + MuJoCo 3.9.0 + Python 3.11**, forced by the **RTX 5090 (Blackwell sm_120)** which needs IsaacSim ≥5.0 (4.5 has no Blackwell support; proven — 4.5 tops at Ada, driver 535.x).

**Migration branch `migrate/isaacsim-5.1-mujoco-3.9` in /tmp/SIMPLE_migrate (5 commits):** pyproject bumps; `omni.isaac.* → isaacsim.*` import rename (Single{Geometry,Rigid,XForm}Prim aliasing — the de-suffixed 5.x classes are tensorized); API-audit fixes (camera frame key 'rgb' not 'rgba', `set_local_pose` wants wxyz, aperture `maintain_square_pixels=False`, `mj_id2name` not `model.id2name`, renderer 'RaytracedLighting', webrtc fallback); **HTTPS submodule URLs** (the 5 git@songlin are PUBLIC — forks diverged so kept, not substituted); **MuJoCo-3.9 `attach(prefix="")`** (3.9 namespaces attached bodies with '/' → broke `torso_link` lookup). Full diff: Neural-Screw-Network reports/SIMPLE_isaacsim51_migration.patch; plan+audit+env-setup in reports/SIMPLE_*.md.

**Envs (conda):** `simple51` (py3.11: isaacsim 5.1.0.0, mujoco 3.9.0, torch 2.7.0+**cu128** [MUST force cu128 for sm_120; isaacsim pulls cu126], numpy 1.26, curobo built from submodule, **warp-lang 1.9.1** [curobo's 1.14 lacks `warp.context`; isaacsim bundles 1.8.2], + transforms3d/fcl/dotenv/opencv/lerobot). `dpserve` (py3.11: torch 2.8 cu128, diffusers 0.30.2, transformers 4.44.2, accelerate, fastapi) for the OFFICIAL DP serve. Runtime: `OMNI_KIT_ACCEPT_EULA=YES` + `LD_PRELOAD=/usr/lib/x86_64-linux-gnu/libgcc_s.so.1`. submodules all public HTTPS.

**Official DP serve:** Psi0 repo (github physical-superintelligence-lab/Psi0) `src/dp/deploy/dp_g1_serve_simple.py --run-dir <ckpt> --ckpt-step 40000` (FastAPI :22085), checkpoint USC-PSI-Lab/psi-model dp/diffusion-policy-g1-sim (ckpt_40000/model.safetensors, loaded strict; NOT ema_net.pth). My reconstruction serve_dp_official.py (resnet18-BN+ConditionalUnet1D, exact weight load) gave the SAME result.

**RESULT (BendPickMP level-0, 3 ep): eval RUNS end-to-end on 5.1** — official serve served 150 /act queries (200 OK), all crashes fixed. **Score 0/3 for BOTH the official serve AND my reconstruction** → 0/3 is the **ENVIRONMENT, not the model/server**.

**ROOT CAUSE CORRECTED (was wrong before): the convex-cook bug is COSMETIC, not the cause.** Code proof (isaacsim.py __create_object): the manipulation object has rigid-body physics AND collision DISABLED (`disable_rigid_body_physics()` + `set_collision_enabled(False)`); __update_object drives its pose from MuJoCo each step (`set_local_pose(obj_info.pose...)`, all physics-enable blocks commented). So the object is a pose-driven visual — it never uses IsaacSim collision; the 12 "Unable to create convex mesh" errors (PhysX 5.6.1 convexHull cooker: eZERO_AREA on sub-mm-scaled COACD pieces + 255-vtx limit on collision_3; fix would be convexDecomposition, schema-verified) change nothing.

**ACTUAL 0/3 CAUSE = a 1-LINE camera-orientation regression — FIXED (commit 982acf2).** The DP is VISUOMOTOR (obs = egocentric head_stereo_left RGB). The migration added a quaternion REORDER at isaacsim.py __update_cameras `set_local_pose(p,[q[-1],q[0],q[1],q[2]])` on a FALSE premise — `cameraEntity.pose.quaternion` is ALREADY scalar-first wxyz (core/types.py) and IsaacSim 5.1 Camera.set_local_pose ALSO expects wxyz (installed camera.py:677, default camera_axes='world'); the convention did NOT change 4.5→5.1 (old path = deprecation shim to the same class). Original 4.5 code passed `q` unreordered (reorder commented out) and scored 10/10. The reorder rolled every camera 180° (head-cam identity [1,0,0,0]→[0,1,0,0] = 180° about X) → flipped OOD obs → policy fails. **FIX = revert to `set_local_pose(p, q)`** (one line). The "dark/black render" was largely an ARTIFACT of the flip (glossy table at grazing flipped angle), resolved by the same fix. Object/robot/light set_local_pose calls already pass wxyz correctly — unchanged. Found via 8-agent transformation-audit workflow (vs `git show d010f0f:...isaacsim.py` + 5.1 source).

**FULL render-transform debug (user-judged via rendered experiments /tmp/render_cam_experiment.py — boots Kit once, no policy/rollout; user's poor-visual-encoder caveat → render + let user judge). FOUR coupled OpenUSD-5.1/migration regressions, all FIXED, head obs now matches 4.5 reference (/tmp/obs_check/final_head.png):**
1. **Camera 180° reorder** (commit 982acf2): migration added `set_local_pose(p,[q[-1],q[0],q[1],q[2]])` — removed (Pose.quaternion already wxyz).
2. **Scene up-axis DOUBLE rotation** (4e86668): OpenUSD 0.24.5 auto-inserts `xformOp:rotateX:unitsResolve` (+90°X, Y-up HSSD→Z-up stage) on `add_reference_to_stage`; SIMPLE ALSO applied rotateXYZ=(90,0,yaw) → net Rx(180) (room flipped). Stripping the auto op FAILS (USD re-inserts on recompose). FIX = drop SIMPLE's X (set rotateXYZ=(0,0,yaw)), keep USD's native unitsResolve.
3. **Camera convention = 'world'** (4e86668): default camera_axes='world' is correct once the scene up-axis is right; interim 'ros' was a red herring from the flipped room.
4. **Objects invisible/oversized — ROOT CAUSE (commit ba13d89, supersedes the wrong x100 in 9fe0881):** NOT core OpenUSD. **IsaacSim 5.1 `SingleXFormPrim.__init__` bakes the referenced asset's metersPerUnit (graspnet=0.01) into xformOp:scale** (4.5's omni.isaac XFormPrim did NOT). A PLAIN USD reference gives scale=(1,1,1)/real-size; `SingleXFormPrim(path)` then sets scale=0.01. SIMPLE constructs XFormPrim on each object SEVERAL times (reset+create+update+L775) → 0.01^n → objects shrink to invisible (or, with my earlier uniform x100, blow up to block the camera — different objects compound differently). **graspnet mesh coords are already METERS** (/Meshes/visual extent = real sizes; metadata mpu=0.01 is WRONG). FIX = pass `scale=[1.0,1.0,1.0]` (identity) to EVERY object XFormPrim ctor so the wrapper doesn't re-scale. Verified real-size AABBs (cracker 0.089×0.171×0.214). NOTE: this is the IsaacSim-RENDER object (what the policy SEES); the MuJoCo physics object is separate + always correct — so the bug corrupted the visuomotor OBS, not the grasp target. Pinpoint tool: add_reference→SingleXFormPrim step-by-step scale dump.

**MuJoCo REVERTED 3.9.0→3.3.6 (commit-pending): only IsaacSim needs the upgrade (5090); MuJoCo's bump was gratuitous + changed contact defaults SIMPLE was tuned on → grasped objects bounced out.** Changelog audit (3.3.6→3.9): multiccd default-on @3.8 (mjDSBL_MULTICCD), native-CCD pipeline (mjDSBL_NATIVECCD), midpoint integration @3.7 for implicitfast free bodies — all 3.9 defaults SIMPLE inherited (sets no flags). Reverting restores validated physics. Audited all 3.9 mujoco mods: attach(prefix="") REVERTED to original attach() (3.3.6 attach()==attach(prefix="")==no-prefix, verified); mj_id2name KEPT (MjModel.id2name doesn't exist in 3.3.6 either — original latent bug); pyproject→mujoco==3.3.6; dm-control 1.0.21 compatible. Official-DP grasp 1/10(3.9)→2/10(3.3.6) (small N). Object contact params (mujoco.py _build_object): condim=4, friction=[0.8,0.05,0.005], solref=[0.005,2], mass=0.1/num_convex; opts impratio=10/cone=elliptic/noslip=2/implicitfast.

**LIGHTING fix: filmIso=85 (env SIMPLE_FILM_ISO).** 5.1 RTX renders ~3-6% brighter in shadows/mids than 4.5 ref (obs mean 190@iso100 vs ref 183.6). Swept: ACES tonemap op=6 is the CORRECT curve (only op matching ref highlights p75≈234; op1 blows, op5/7 crush); GI/histogram-autoexposure NOT the cause. filmIso (exposure) is the knob: iso70=172/iso80=179/iso90=185/iso100=190 → iso85≈183 (user-picked). Set in isaacsim.py _config_isaac via carb /rtx/post/tonemap/filmIso.

**USER's policies (mshab-state branch): PsiG1DiffusionPolicy, serve via src/scripts/serve_psi_g1.py (port 21000, same /act numpy-JSON protocol as dp_g1 client), env p312 (torch2.9/diffusers0.36/curobo).** Checkpoints psi_g1_exps/psi-bendpickMP-{unet,fk_feature}-s2026/models/latest.pt (step 40k, image_size=128 LOW-RES, obs_h1/pred16/act6, fk_backend=curobo). USER reports HIGH success on old isaac-5.0 + broken render → policy is state-robust (not render-bound). Eval: serve_psi_g1 --ckpt … --data-root ~/psi_data/simple/G1WholebodyBendPickMP-v0 --image-size 128 --port P; then simple.cli.eval … dp_g1 --port P. 3-policy sweep (official/unet/fk_feature) running at iso85.

**FINAL EVAL (BendPickMP level-0, 10 ep, corrected sim: mujoco3.3.6 + camera/scene/object-scale fixes + iso85): object-scale root fix was the big unlock.** official DP 2/10→**9/10** (≈ paper 10/10 → MIGRATION VALIDATED), user unet 2/10→**4/10** (works; gap = 128 vs 224 res handicap), user fk_feature 0/10→**0/10 (UNCHANGED)**. fk_feature 0/10 is now a **fk_feature-SPECIFIC bug, NOT the sim**: unet runs same env+image+state→4/10, but fk_feature (adds curobo-FK-from-state feature) is actively corrupted→0/10. Suspect = curobo FK in the SERVE (serve_psi_g1.py, p312) producing OOD features vs training (p312 robot-model warnings: 'TrackedArray has no attribute ptp' = numpy2.x/trimesh; truncated mesh filenames). Serve returns valid (non-NaN) actions, so it's a feature-distribution mismatch, not a crash. NEXT (offered): diff serve-time curobo FK feature vs training-time (eval_psi_g1_offline.py).

KEY PATTERN: OpenUSD 0.24.5 (5.1) does AUTOMATIC up-axis + units resolution on reference that 4.5's older USD did NOT — SIMPLE's manual conversions now double up. **The convex-cook (PhysX 5.6.1) is COSMETIC** (object collision+rigid physics disabled, pose-driven from MuJoCo). MuJoCo does NOT place the HSSD room (IsaacSim-render-only); writes layout actor poses from mjData (wxyz). Obs source = IsaacSim render (isaacsim.py:953 get_rgba; dp_g1.py:82). IsaacSim 5.1 comps: PhysX 5.6.1, omni.physx/PhysxSchema 107.3.26, OpenUSD pxr 0.24.5, warp 1.8.2. Branch migrate/isaacsim-5.1-mujoco-3.9 now 8 commits (…9fe0881). NEXT: eval for success rate (obs now verified-correct). See [[project_g1_humanoid_psi]].
